@model IEnumerable<weatherescu_test_2.Models.City>

<h2 class="text-center">Cities & Precipitation</h2>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>City</th>
            <th>Country</th>
            <th>Precipitation (mm)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var city in Model)
        {
            <tr>
                <td>@city.Name</td>
                <td>@city.Country</td>
                <td>@(city.Precipitation?.Value ?? 0) mm</td>
            </tr>
        }
    </tbody>
</table>
